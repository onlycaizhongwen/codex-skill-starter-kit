# 新项目接入手册

这份手册用于指导团队把 `codex-skill-starter-kit` 接入一个新的 Codex 项目，并在真实研发场景中稳定使用。

## 一、接入目标

接入完成后，项目会具备以下能力：

- 使用项目根 `AGENTS.md` 作为项目级规则入口
- 使用 `project-entry` 作为统一入口技能
- 自动把任务分流到开发约束、基础设施查询、业务域文档、长任务管理、需求分析、设计、计划、追踪等技能
- 在长任务和可中断任务中保留 `.codex/plans/` 持久化记录
- 在子代理协作时尽量继承同一套项目规则
- 可以直接参考 `examples/minimal-project/` 演示自动识别与分流效果

## 二、准备条件

请确认：

- 本机已经安装 Codex
- 本机已经可以使用 `python`
- 如果本机装有多个 Python，建议优先使用 `py -3`
- 你已经拿到本 starter kit 目录

假设 starter kit 路径为：

```text
d:\czw_cloude\codex-skill-starter-kit
```

## 三、全局安装技能

先把技能安装到当前用户的全局 Codex 技能目录：

```bash
python d:\czw_cloude\codex-skill-starter-kit\scripts\install_starter_kit.py --overwrite
```

如果你同事机器上的 `python` 不是 Python 3，改用：

```bash
py -3 d:\czw_cloude\codex-skill-starter-kit\scripts\install_starter_kit.py --overwrite
```

如果只想先预览：

```bash
python d:\czw_cloude\codex-skill-starter-kit\scripts\install_starter_kit.py --dry-run
```

## 四、初始化项目入口

进入你的项目根目录后执行：

```bash
python d:\czw_cloude\codex-skill-starter-kit\scripts\install_starter_kit.py --overwrite --init-project .
```

这一动作会：

- 安装或覆盖全局技能
- 在当前项目根目录生成 `AGENTS.md`
- 初始化 `docs/codex/v1/requirements/.gitkeep`
- 初始化 `docs/codex/v1/designs/.gitkeep`
- 初始化 `docs/codex/v1/plans/.gitkeep`
- 初始化 `docs/codex/v1/trace/.gitkeep`
- 初始化 `docs/codex/v1/status.md`
- 初始化 `.codex/plans/main/TASKS.md`

如果项目里已经有 `AGENTS.md`，且你确认要覆盖：

```bash
python d:\czw_cloude\codex-skill-starter-kit\scripts\install_starter_kit.py --overwrite --init-project . --project-overwrite
```

## 五、接入后的典型项目结构

```text
<你的项目>/
├── AGENTS.md
├── docs/
│   └── codex/
│       └── v1/
│           ├── status.md
│           ├── requirements/
│           ├── designs/
│           ├── plans/
│           └── trace/
└── .codex/
    └── plans/
        └── <branch>/
            ├── TASKS.md
            └── <task>/process.md
```

说明：

- `AGENTS.md`：项目级规则入口
- `docs/codex/v1/`：需求、设计、计划、追踪文档区
- `.codex/plans/`：长任务持久化记录区

## 六、第一次进入项目时怎么说

进入项目后，不需要记忆所有技能名，直接按任务目标描述即可。下面这些说法都适合：

```text
请按项目入口规则初始化当前项目工作流，并补齐 AGENTS.md、docs/codex/v1 骨架和 .codex/plans/main/TASKS.md。
```

```text
请按项目规则分析这个需求并建立对应文档。
```

```text
请按项目规则继续实现这个功能，需要时自动接入长任务记录。
```

```text
请按项目规则恢复上次中断的任务并继续推进。
```

## 七、真实项目中的任务示例

### 1. 后端缺陷修复

适合场景：

- 线上 bug 排查
- 接口异常处理修复
- 定时任务重复执行问题
- 配置错误导致的行为偏差

推荐提法：

```text
请按项目规则排查“退款回调偶发重复处理”的问题，先定位根因，再修改代码并给出验证结论。
```

典型技能流转：

```text
AGENTS.md -> project-entry -> dev-constraints
```

如果是多轮排查、可能跨多天推进：

```text
AGENTS.md -> project-entry -> dev-constraints -> task-control
```

### 2. 新需求拆解为 requirements

适合场景：

- 产品口头描述较粗
- 需求边界不清晰
- 需要先形成结构化需求文档

推荐提法：

```text
请按项目规则分析“支持分账撤销后自动回补余额”这个需求，输出 requirements 文档，并标出正常流、异常流和边界条件。
```

典型技能流转：

```text
AGENTS.md -> project-entry -> project-skill-convention -> req-analysis
```

产出通常会进入：

```text
docs/codex/v1/requirements/
```

### 3. 从 requirements 进入技术设计

适合场景：

- 已经有需求文档
- 需要输出接口、表结构、时序、兼容策略

推荐提法：

```text
请基于现有 requirements，为“分账撤销后自动回补余额”输出技术设计，重点说明事务一致性、幂等和补偿策略。
```

典型技能流转：

```text
AGENTS.md -> project-entry -> project-skill-convention -> design-phase
```

产出通常会进入：

```text
docs/codex/v1/designs/
```

### 4. 从设计进入执行计划

适合场景：

- 设计已定
- 需要拆实现步骤
- 需要明确验证策略和风险

推荐提法：

```text
请按项目规则把这个设计拆成可执行计划，明确每个阶段的目标、改动范围、验证方式和风险提示。
```

典型技能流转：

```text
AGENTS.md -> project-entry -> project-skill-convention -> plan-phase
```

产出通常会进入：

```text
docs/codex/v1/plans/
```

### 5. 跨多轮的长任务推进

适合场景：

- 多文件、多模块改动
- 一次对话完成不了
- 需要明确恢复点

推荐提法：

```text
请按项目规则把“会员结算链路重构”注册成长任务，先建立任务记录，再从第一阶段开始推进。
```

后续恢复时可以说：

```text
请按项目规则恢复“会员结算链路重构”这个长任务，读取已有记录后继续未完成部分。
```

典型技能流转：

```text
AGENTS.md -> project-entry -> dev-constraints -> task-control
```

记录通常会进入：

```text
.codex/plans/<branch>/
```

### 6. 查询基础设施信息

适合场景：

- 查 MySQL 表结构与索引
- 查 PostgreSQL 字段定义
- 查 Elasticsearch mapping
- 查 Nacos 配置
- 查链路追踪或组件依赖

推荐提法：

```text
请按项目规则帮我确认 settlement_order 表结构、主索引、唯一约束，以及对应服务在 Nacos 里的核心配置项。
```

典型技能流转：

```text
AGENTS.md -> project-entry -> dev-small-tool
```

### 7. 初始化业务域知识文档

适合场景：

- 新项目接入
- 业务域复杂但缺少长期说明
- 团队希望沉淀领域上下文

推荐提法：

```text
请按项目规则梳理“清结算域”的核心对象、关键流程、状态流转和上下游依赖，初始化长期业务说明文档。
```

典型技能流转：

```text
AGENTS.md -> project-entry -> docs-desc-generation
```

产出通常会进入：

```text
docs/desc/
```

### 8. 实现完成后的闭环审查

适合场景：

- 功能快做完了
- 需要检查需求、设计、计划、实现是否一致
- 需要看是否还有缺口或遗漏测试

推荐提法：

```text
请按项目规则审查“分账撤销后自动回补余额”是否已经形成闭环，检查 requirements、design、plan、实现和验证记录是否一致，并指出缺口。
```

典型技能流转：

```text
AGENTS.md -> project-entry -> project-skill-convention -> req-trace
```

产出通常会进入：

```text
docs/codex/v1/trace/
```

## 八、用示例项目快速演示

如果你想先给团队做一轮低成本演示，可以直接使用：

```text
examples/minimal-project/
```

推荐演示步骤：

1. 进入示例项目目录。
2. 先输入“请按项目入口规则初始化当前项目工作流，并补齐 AGENTS.md、docs/codex/v1 骨架和 .codex/plans/main/TASKS.md”。
3. 再从示例项目 `README.md` 里复制一个任务示例。
4. 观察是否能自动进入对应技能链路。

## 九、子代理协作怎么用

如果任务较大、需要拆给子代理，建议主代理遵守这几点：

1. 先由 `project-entry` 判断当前任务属于哪一类。
2. 先确认是否需要接入 `task-control`。
3. 派发时一起传递以下信息：

- 当前任务目标
- 子任务边界
- 项目根 `AGENTS.md` 继续生效
- 子代理必须遵守的技能
- 必须先读的文档或记录
- 回传格式：改动文件、验证结果、风险、下一步

推荐提法：

```text
请按项目规则把这个任务拆成可并行的子任务，并确保子代理继续遵守 AGENTS.md 和当前任务记录。
```

## 十、团队内部分发方式

建议一起分发以下内容：

- `codex-skill-starter-kit/` 整个目录
- 本手册
- `examples/minimal-project/`

对方通常只需要执行两步：

```bash
python scripts/install_starter_kit.py --overwrite
python scripts/install_starter_kit.py --overwrite --init-project <项目目录>
```

如果对方机器的 `python` 版本不确定，建议直接发这一组：

```bash
py -3 scripts/install_starter_kit.py --overwrite
py -3 scripts/install_starter_kit.py --overwrite --init-project <项目目录>
```

## 十一、常见问题

### 1. 只安装全局技能，不初始化项目，可以吗？

可以，但不推荐。

这样虽然还能手动使用技能，但没有项目根 `AGENTS.md`，项目级自动入口会明显变弱。

### 2. 项目里还没有 `docs/codex/`，怎么办？

正常。第一次走需求分析、设计、计划、追踪流程时，会逐步初始化对应目录。

### 3. 长任务记录写到哪里？

默认写到：

```text
.codex/plans/<branch>/
```

### 4. 为什么有时终端里看起来像乱码？

这通常是 Windows PowerShell 的编码页显示问题，不代表文档文件损坏。
当前脚本生成的初始化文件已经按 UTF-8 BOM 写入，兼容性比普通 UTF-8 更好，但终端显示仍可能受本机编码页影响。

更可靠的判断方式是：

- 用 UTF-8 友好的编辑器打开文件
- 以安装脚本实际运行结果为准
- 不把单次终端显示乱码等同于文件内容错误

## 十二、推荐最小接入流程

对于全新项目，推荐按下面顺序接入：

1. 安装全局技能。
2. 在项目根初始化 `AGENTS.md`。
3. 让 Codex 按项目入口初始化文档区。
4. 先做一条真实需求的 requirements。
5. 再走 design -> plan -> 实现 -> trace 全链路。

## 十三、建议演示口令

你可以在一个新项目里直接复制下面几句做最小演示：

```text
请按项目入口规则初始化当前项目工作流，并补齐 AGENTS.md、docs/codex/v1 骨架和 .codex/plans/main/TASKS.md。
```

```text
请按项目规则分析“新增用户注册审核流”这个需求，并建立 requirements 文档。
```

```text
请按项目规则基于现有 requirements 输出技术设计和执行计划。
```

```text
请按项目规则继续实现这个需求，需要时自动接入 task-control。
```

```text
请按项目规则审查这个需求是否已经闭环。
```

## 十四、附加资料

建议和 starter kit 一起分发以下文件：

- `README.md`
- `新项目接入手册.md`
- `examples/minimal-project/`

## 十五、本地维护能力

当前 starter kit 还包含一个本地维护技能：

- `session-cleanup`：安全清理旧 Codex 会话与可删除的临时残留

它不参与项目主流程，只用于本机环境维护。
